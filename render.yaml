services:
  - type: web
    name: eduaccess-ai-backend
    env: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: CORS_ORIGINS
        value: "https://eduaccess-ai.vercel.app,http://localhost:3000"
      - key: PYTHONUNBUFFERED
        value: "1"
